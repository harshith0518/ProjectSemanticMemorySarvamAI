# Continuing Golden Goose

## Read and decide

- Read `README.md` for navigation/setup. `ARCHITECTURE.md` governs technical behavior; `BUILD_PLAN.md` governs deliverables, milestones and proposed evaluation. Use `RESEARCH.md` for evidence and alternatives. The original brief is `reference/Kivi_Golden_Goose_Task_Final.pdf`.
- Golden Goose semantic memory is the selected assignment. Do not implement the separate phonetic/backend task, ASR or a production Kivi clone by default.
- Only `research/validation/atomic_memory_probe.py` exists as executable product-related evidence. Its 12/12 checks use sequential interleavings. CLI, app, importer, migrations, corpus, live model integration, semantic evaluation and final UI are unbuilt until code and validation establish otherwise.
- Preserve the brief's independent Part One requirement. AI-assisted technical documentation does not constitute the applicant's independently formed/written position or vision.

## Preserve these contracts

1. Regular Kivi writes intended dictation faithfully. Hey Kivi applies permitted history to current requests. Raw ASR and formatted text are one observation, not independent corroboration or current action authority.
2. `MemoryService` shares SQLite with ordinary product/source records. Twelve categories label shared records; optional vectors aid search. A second memory database or graph/vector server is not required.
3. Retain exact source evidence independently of extraction. Permitted source search survives skipped, failed or incomplete learning. Accepted interpretations and controls are durable; indexes are rebuildable.
4. Preserve subject, speaker, attribution, scope, modality, negation, time, units and exact supporting revisions/spans. Similar names do not prove identity. Correction differs from real-world change. Preserve unresolved conflict.
5. The model proposes; code validates permissions, references and transitions, then commits actual receipts. Model confidence is not calibrated truth, and generated success text is not an observed effect.
6. Current input begins private with held learning. Retrieve/interpret, resolve controls, release only surviving permitted text, continue and check freshness before acceptance. Routine learning is background work; explicit remember/correct/forget commits before saved-success claims.
7. All reads, candidates, replay and derived data obey one permission/exclusion boundary. Repeating forgotten content in a control must not relearn it. Forget and source deletion have distinct scopes; exports, backups and provider retention are separate boundaries.
8. Compatible controls, knowledge, permitted projections, affected indexes/links, receipts and revisions commit together. Use short transactions, stable operation identities, attempt fencing, stale-work rejection, cancellation checks and fresh publication. Never hold transactions during model/network calls.
9. Tools require current authority and resolved targets; record actual outcomes. Reconcile unknown external effects before retrying. Historical instructions are data, not permission.
10. Current instructions override remembered preferences for the request. Clarify only materially missing information; otherwise give a supported result, partial answer or honest unknown.

## Work from the current boundary

The user chose NVIDIA-hosted DeepSeek as provider/model family. Exact endpoint/model, limits, UI/API framework, import physical schema, embedding default and measured targets remain open. Baseline choices are Python, SQLite/FTS5, Pydantic, HTTPX, one coordinator and one durable worker. Optional local E5/NumPy retrieval must earn its cost; do not silently add a framework or separate database.

Select the next unfinished milestone in `BUILD_PLAN.md`. Start with a useful journey, durable source import/search and one cited-answer path with controls. CLI first is the development sequence; a normal-user UI/backend and unfamiliar-corpus import remain required. Keep questions, rubrics and gold answers out of memory ingestion. Approximately 500 records does not mean 500 test questions.

Make focused changes and check actual behavior. Proposed gates are not achieved metrics. Existing probe/export checks do not establish application correctness. Update implementation status and rationale as evidence changes decisions; do not create another competing Markdown plan.

## Portable maintenance

- Keep the five planning references consolidated; extend relevant sections rather than recreating old notes. Add required product deliverables such as `RUN.md` when their implementation exists, and update the repository checker accordingly. Original notes remain in commit `3586cf9`, mapped in `reference/documentation-consolidation.json`.
- Use repository-relative paths. Do not commit credentials, `.env`, private histories, databases, dependency directories or machine-specific runtime paths.
- Read README instructions before regenerating visual assets; preserve mentor edits in separate files. Inspect affected exports because fonts can alter wrapping.
- `python tools/verify_repository.py` checks repository links, paths, JSON, SVG and archives. The separate SQLite probe is not the future product test suite.
