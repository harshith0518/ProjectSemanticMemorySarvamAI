{
  "name": "golden-goose-architecture-docs",
  "version": "1.0.0",
  "private": true,
  "description": "Optional local PNG rendering for the Golden Goose planning artifacts",
  "scripts": {
    "render:flow": "node render_mentor_flow.cjs",
    "render:svg": "node mentor-svg/render_svgs.cjs",
    "render:board": "node mentor-excalidraw/render_previews.cjs",
    "render": "npm run render:flow && npm run render:svg && npm run render:board"
  },
  "dependencies": {
    "sharp": "0.35.4"
  },
  "engines": {
    "node": ">=20.9.0"
  }
}
