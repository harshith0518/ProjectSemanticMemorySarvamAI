# Delivery tracker

Updated: 22 September 2026. The submitted project is now being prepared for the shortlisted interview.

This is the single current-status checklist. [PLAN.md](PLAN.md) defines scope and milestone gates; [DECISIONS.md](DECISIONS.md) records decisions; [EVALUATION.md](EVALUATION.md) defines evidence. Check an item only after its acceptance condition is demonstrated.

**Current work:** The healthy rebuilt local interface runs at **http://127.0.0.1:18000** with Google `gemini-3.5-flash-lite`; `/ready` reports schema `0006_turn_assessment`. Clear public/date questions stay only in the current chat; contextual messages preserve originals and selectively learn new assertions, repeats and updates. Automatic recall now starts with active learned memories and their supporting originals instead of serializing every note in a small collection; only an explicit inventory uses complete small-collection review. The metrics panel turns evidence/context, token, call and phase timing data into readable cards and tables. Citations use meaningful source labels, with immutable IDs retained only in Technical reference. `qwen3:4b` is an explicit host-local Ollama backup; Google remains default and the installed embedding model is not yet a retrieval dependency. The complete isolated suite passed **369 tests in 136.13 seconds**; Ruff, formatting, TypeScript and Vite build passed. The aggregate browser run completed 18 journeys then stalled at delayed Private-learning cancellation, so the complete browser acceptance is still open. The retained **`interview-chat-20260922`** demo has six originals, two active memories and four revisions; **`routing-facts-20260922`** adds a scoped Willow preference and duplicate check. Private remains context-free with no database read/write. [Latest routing evidence](eval/reports/interview-question-routing-review.json), [conversation evidence](eval/reports/interview-conversation-review.json).

## Resume point for the next chat

- [ ] **Finish browser acceptance honestly.** Bring up only `compose.test.yaml`'s browser profile, isolate `Private cancels pending learning` in `tests/browser/workspace.test.mjs`, and determine why its delayed cancellation case stops making progress. It is currently neither a passing check nor a confirmed product defect. When it has a clear result, rerun the complete browser suite before describing the UI milestone as fully verified. The exact commands and the prior 18 completed journeys are in [RUN.md](RUN.md#browser-acceptance-resume-point).

- [x] **Consolidate the interview hardening implementation documentation.** README now maps the assessment, learning, retrieval, provider, frontend, migration and test entry points; ARCHITECTURE/DECISIONS record bounded non-vector recall, source labels and manual Ollama backup; EVALUATION/RUN preserve the passing backend/build evidence and the incomplete browser run. The local Ollama transport probe is intentionally kept distinct from a quality evaluation.

## Completed

- [x] **Public-question routing and selective evidence repair.** Fixed the exact capital-question failure caused by trailing request framing; clear public/date questions now stay in the current chat and skip extraction. Automatic retrieval filters completed no-memory chat history, retaining claim support and pending/failed originals. Context counts describe actual provider input; model-output validation failures no longer blame the user input. Preserved old source history and tested personal/mixed learning, private gates and retries. Passed 310 PostgreSQL tests, 33 browser journeys, builds and lint/format checks. Live capital/planets used zero saved evidence; beverage recall retained its condition/citation; a fictional Willow preference learned once and repeated without duplication. [Evidence and limits](eval/reports/interview-question-routing-review.json).

- [x] **Readable storage sizes.** Usage storage cards and answer evidence metrics share decimal bytes/KB/MB/GB formatting with up to two decimal places and exact-byte hover text. Verified ten representative/boundary values, TypeScript, formatting, production/Docker builds and the healthy live Usage page (119.5 KB originals, 169.54 KB structured memories, 30.69 KB passages). This display-only change made no model calls or changes to measured storage.

- [x] **Automatic learning from Normal Ask.** Added idempotent user-message capture, source-specific processing through the shared worker, six-message bounded user context, add/repeat/update/no-memory outcomes, explicit learning retries and failure isolation, source/history links and separate metrics. Research used Mem0 and LangMem's primary repositories; no additional memory-store dependency or schema migration. Assistant output is not ingested; Private capture/learning is rejected before body parsing/store access. Improved quote-only examples and structural repair feedback after retaining an initial live update failure. Final checks: 295 PostgreSQL tests, 32 browser journeys, lint/format/TypeScript/build, isolated scoped reset and six live turns. The actual scope-qualifier weakness is recorded rather than treating successful structural validation as semantic certification.

- [x] **Automatic context and labelled fallback.** Implemented the user's requested retrieval repair and public-knowledge fallback. Complete small-collection review recovered tea/beverage paraphrases, Atlas/Orion mentions and inventories. Existing strict representations remain available for comparison. Date responses use the application clock, unknown personal facts stay unknown, large-collection coverage is explicit, and general answers are not saved or dressed as citations. Initial live review exposed a conflict-summary preference and an unclear weather explanation; both were refined and retested rather than hidden. See the curated review for final checks, actual model-call metrics and remaining limits.

- [x] **Interview demo input and processing repair.** Saved notes now refresh both Sources and Memory status immediately and pending/failed records appear on Memory as stored sources, never as learned facts. A separate default-off fictional-source switch permits the user-approved local dummy records. Private now provides a context-free direct model composer with a Submit button, Enter-to-send, Shift+Enter for a newline, transient model/token/latency metadata and no database read/write. Normal Ask also sends with Enter and clears its composer. The retained `my-notes` queue recovered from 1 failed / 13 pending / 1 succeeded to 16 succeeded / 0 pending / 0 failed. Live browser checks covered the pending card, one-source processing, evidence-backed Ask and Private direct chat; isolated focused checks passed 94 tests with zero warnings, Python lint/format and the production frontend build passed.

- [x] **Flexible questions over approved synthetic evidence.** Added a default-off Google/OpenRouter question approval independent of source/reviewer consent. The backend still checks every retrieved source against the approved synthetic corpus before provider access; Private, citation, budget and no-fallback controls are unchanged. The production frontend build passed, 44 focused PostgreSQL checks passed without warnings, Ruff lint/format passed, the preserved API/database restarted healthy, and `Who is Atlas?` completed through Google in 5.854 s using seven database sources and 1,574 input / 55 output tokens.

- [x] **Restore the current local interface and record the handoff audit.** Recreated only the local API with the already-tested image, verified healthy API/database and configured Lightning, and inspected desktop/mobile screenshots without new model calls. Recorded the failed browser evidence-selector preflight and the blocked full-corpus attempt, rather than treating them as passes. [Actual PDF tally, limits and proposed local scope](eval/reports/s13-local-readiness-audit.json). API limits are temporarily tightened to the trusted pilot pending explicit reconciliation; broader workflow work and final handoff remain open.

- [x] **Recheck and retain Lightning as a manual backup.** Three fresh provider completions produced one stored memory after a validation repair and one correctly cited revised-date answer. Recorded per-call tokens, model timings, end-to-end timings, the rejected first proposal and the still-disabled legacy browser configuration. [Actual evidence and limits](eval/reports/s12-lightning-backup-review.json), [manual backup instructions](RUN.md#lightning-manual-backup-checkpoint). No automatic fallback, runtime activation, budget reset or new privacy exception.

- [x] **Preserve the supplied Part One Google Doc mechanically.** Saved the complete text export and unchanged positioning/vision excerpts, with source URL, SHA-256 hashes and explicit counting boundaries. The excerpts contain 96 and 387 words; the vision plus following Q&A contains 711. [Actual preservation record](eval/reports/part-one-google-doc.json). Substantial overlap with earlier AI-assisted drafts means independent authorship remains unverified; this does not close Part One's independent-writing gate.

- [x] **Free-provider integration and capped comparison.** Added explicit default-off Google/OpenRouter synthetic-only adapters using the same guarded accounting, zero-price OpenRouter routing without fallback, and clear rate-limit errors. Full isolated PostgreSQL and browser suites passed. Retained all 20 live comparison calls and semantic failures in [the review](eval/reports/s12-free-provider-review.json). This closes the diagnostic implementation, not full-corpus evaluation or the live-quality gate. Per-role Compose model overrides still need the proposed repair; do not infer that an environment override is forwarded merely because the adapter supports it.

- [x] **Evening evaluation preparation and bounded LLM check.** Added the 540-record reproducible synthetic corpus, 84 non-ingested cases, 30 preselected showcase questions, bounded relevant-memory selection, explicit reviewer consent/budgets, official Kivi bird asset and simpler evidence controls. Fixed the test responder identity and Conversation-scoped browser locator with explicit approval. All 244 backend and 24 browser checks passed; earlier failures remain documented. Lightning access works but semantic quality remains open. Ultra's separate ten-call comparison is complete, not selected for application use, and retains every result and failure. [Implementation evidence](eval/reports/s11-implementation.json), [Ultra review](eval/reports/s11-ultra-review.json). The full live corpus and final submission gates are not complete.

- [x] **Tonight setup/readiness checkpoint.** Restarted Docker Linux, rebuilt the pinned image, restored the retained application DB/API and opened the browser. Re-ran 232 backend/22 browser checks, builds, formatting, lint and readiness/drift checks. Fixed only the retrieval evaluator's shared test-owner identity; both independent and concurrent-worker 500-record diagnostics recovered all required evidence in 45/45 answerable attempts per representation. Retained the initial evaluator failure, two new failed Kimi probes and the structurally accepted but semantically flawed Nemotron probe. [Evidence](eval/reports/tonight-readiness.json). Model/provider settings, privacy semantics, runtime schema and the lifetime allowance were not changed; the next provider/prompt repair is proposed, not implemented.

- [x] **HTML progress and interview guide (12 September).** Created the requested [local interactive guide](docs/project-progress.html) from the current tracker, design documents, code references and recorded evidence at application baseline `1756069`. Includes all 12 milestone statuses, a complete 12-stage input-to-memory diagram, storage/evidence mapping, four worked source examples with actual live failures, Ask/controls, pending work and 12 interview questions. Static structure, JavaScript syntax and all 81 guide links/anchors passed; [actual checks and limits](eval/reports/project-progress-guide.json). This is a dated documentation view, not a second maintained checklist or a completed live-model gate. No application/database/provider change or live call. Next implementation action remains provider/extraction reliability, then S11.

- [x] Establish the focused implementation plan, architecture, evaluation strategy, review contract and contribution rules.
- [x] Replace the previous repository contents and push the planning baseline to `main`, preserving earlier Git history. Commit: [`596b034`](https://github.com/harshith0518/ProjectSemanticMemorySarvamAI/commit/596b034940ea3a6adabeb3940dd6d793bc2c8b1e).
- [x] Verify that baseline's nine-file remote tree, clean local/remote agreement, Markdown links and staged whitespace. These are documentation checks, not application tests.
- [x] **S02 — Prepare the existing environment.** Started Docker Desktop; verified Linux Docker client/server `28.4.0`, Compose `2.39.2-desktop.1` and Ubuntu-24.04 integration. Pulled the official probe image, verified a named-volume marker survived replacement of a disposable container, and removed the uniquely named probe volume. Keep one Windows checkout and run the planned backend in Linux images. [Actual checks and limits](RUN.md#environment-recommendation); no project DB or application tests yet.
- [x] Record DeepSeek's main-model role and shortlist smaller memory-operation proposers. Public provider documentation was checked; account access, quality and cost remain untested.
- [x] Create this tracker and record the user-requested test → update tracker → commit → push workflow.
- [x] Preserve the latest user-supplied notes and inspect the relevant earlier discussion in **Explain Sarvam AI products** and **Document product features and plan**. Record [provenance and mechanical counts](docs/part-one/README.md): 813 whitespace-delimited tokens overall; 317 in the Part One subsection including questions. These are source notes, not certified independent submissions.
- [x] Commit and push source-note preservation/tracking as [`559f3aa`](https://github.com/harshith0518/ProjectSemanticMemorySarvamAI/commit/559f3aadd8fbb57bdf25760097671dc5da195759). Remote `main` was independently checked at that full commit after the push; staged whitespace passed.
- [x] Commit and push verified Docker/WSL readiness as [`c260e6a`](https://github.com/harshith0518/ProjectSemanticMemorySarvamAI/commit/c260e6af6f45a728da67694d0a87abf80a582bbc); verified clean checkout and exact local/remote commit agreement.
- [x] Prepare [fresh-chat handoff instructions](README.md#starting-a-fresh-implementation-chat), identify supporting resources outside the repository, and preserve the two explicitly requested [AI-assisted chat drafts](docs/part-one/README.md#ai-assisted-drafts-supplied-in-chat). These drafts do not close S01 or approve S03.
- [x] Create the [18-page visual memory guide](output/pdf/kivi-memory-visual-guide.pdf) and its regeneration script. Review rendered diagrams for learning, retrieval, models, Private/Correct/Forget, race conditions, feedback, evaluation and delivery. Record the user's updated workflow: discussion chat with PDF/Markdown, then Codex CLI for approved coding. This is documentation, not application implementation.
- [x] Clarify page 2: the assignment's broad semantic-memory scope includes facts, scoped preferences and useful episodes; automatic procedural learning is deferred by our plan. Add three clickable reference cards to every page and a [reference register](docs/visual-guide-references.md) with exact brief locators, source support and limitations. Align PLAN.md and ARCHITECTURE.md without changing implementation scope.
- [x] Validate the implementation-chat handoff against the repository, parent planning workspace, assignment brief, fixtures, educational site and archived history. Recheck source counts, local links, archive hashes and existing teaching tests. Confirm the clean starting checkout matches remote `main` at `3eca8fb43be8825f4db205d4fe9533c5466e2431`. Docker's Linux engine is currently stopped; the earlier S02 probe remains historical evidence. See the [current environment recheck](RUN.md#implementation-chat-recheck).

- [x] Record the user's `dev`/`main` workflow and prepare the [bounded S03 proposal](PLAN.md#first-implementation-approval-scope) with files, schema effects and acceptance checks. Run the requested read-only CLI session and verify host CLI authentication plus Windows/Ubuntu Docker access; [actual results and sandbox limits](RUN.md#dev-branch-readiness-check). The CLI made no implementation or Git changes. Other-session visual-guide edits remain separate from this readiness/proposal checkpoint; S01 preservation and S03 approval remain open.

- [x] **S03 — Backend bootstrap.** User explicitly approved Python 3.12/uv, Docker, PostgreSQL with pgvector, source/job/policy migrations, shared FastAPI/Typer services and isolated tests after confirming Part One complete in their possession. Implemented the bounded scope; all five acceptance gates passed: clean initial startup, empty/repeated migrations without drift, shared API/CLI behavior, identical synthetic source/job state after replacing both containers, and 23 real PostgreSQL tests with no warnings. Ruff lint/format passed. Actual DB outage/recovery and scoped test reset also passed. [Commands, results and limits](RUN.md#actual-s03-results). No model calls, general imports, retrieval, UI or full memory controls were implemented.

- [x] **S04 — Evidence contracts and policy boundaries.** Explicitly approved and implemented. Typed observations/passages/claim revisions preserve exact variants, scope, attribution, uncertainty, units and unknown times. Backend-owned contexts gate all personal-store operations; Private validation/failure paths show zero connection/SQL/file-write attempts, no input/error logging, unchanged durable records/jobs and no backfill. Supported writes recheck revisions under the owner policy lock; separate PostgreSQL connections/barriers cover both race orderings and competing appends. The additive migration preserves original S03 fields. All 75 tests and Ruff lint/format pass; live API/CLI validation and container replacement pass. [Commands, review and limits](RUN.md#actual-s04-results). General import/models/retrieval/UI/full Correct/Forget remain later work.

- [x] **S05 — Diagnostic import and inspection.** Approved and implemented through shared services. Import all eight synthetic observations with exact variants/metadata, inspect sources and pending jobs, and validate all 11 evaluator excerpts without importing labels. Stable namespace/original-ID keys preserve distinct equal-text observations. Exact retries preserve every source/job field; changed or invalid batches fail atomically. Private refuses body/stdin/store access with zero durable side effects; actual PostgreSQL connections/barriers cover import serialization and policy ordering. All 111 tests and Ruff pass. The documented Windows/API/CLI run exposed and fixed response charset decoding, then verified Unicode and unchanged source/job/S03 probe state across container replacement. No migration/dependency or live-model change. [Commands/results/review](RUN.md#actual-s05-results). User explicitly authorized merging the tested `dev` work into `main` and pushing; verify the remote tips as the delivery step.

- [x] **S03–S05 audit before S06.** Read the current instructions/planning and all 18 PDF pages, mapped diagrams to implementation and later scopes, and recorded stale PDF model/status labels without rewriting the historical snapshot. Fresh image rebuild: 111 isolated PostgreSQL tests passed in 12.56 s with zero warnings; Ruff lint/format and application migration-drift checks passed. Both remote branches matched S05 `a599414b431783661f54aff2c542a11ebeb0e78a` before this audit checkpoint. Three locally configured NVIDIA key names/prefixes were checked without exposing values; credentials/quotas remain untested. User selected Kimi K3 for S06, Nemotron Lightning for S07, and optional Ultra only after a separate comparison decision. [Actual audit and limits](RUN.md#s06-readiness-audit). Commit/push this documentation checkpoint to `dev`, then the explicitly authorized `main` publication, and verify both remote tips.

- [x] **Early S10 source-workspace UI.** Explicitly requested as the primary user workflow after the audit. Plain browser UI uses existing import/list/inspect services; no new application dependency or schema. Transient state, backend policy, fixed errors, safe text rendering, no-store/CSP headers and mode/late-response clearing preserve the bounded privacy contract. Tests and actual runtime checks are recorded in [RUN.md](RUN.md#browser-workflow) and [the evaluator record](eval/reports/ui-foundation.json). Full Ask/Correct/Forget and live-model evaluation are not complete.

- [x] **Input-to-memory research review (12 September).** Reviewed current/parent planning, relevant visual-guide pages and primary memory/database sources; clarified source preservation, selective learning, storage and controlled evaluation in the existing documents. This is design evidence, not S07 implementation or measured model quality. Documentation checks are recorded in RUN.md; publish this checkpoint to `dev` only.

- [x] **S07 implementation and deterministic acceptance.** Approved on 12 September. Shared service/worker/UI processing, typed evidence/reconciliation, lease/receipt fencing and bounded provider accounting are implemented. PostgreSQL/browser checks, migration/persistence and visual review are recorded in [RUN.md](RUN.md#s07-memory-processing). This closes the implementation/contract work, not the live-quality milestone gate. Commit/push to `dev` and verify the remote; merging remains user-controlled.

- [x] **S08 retrieval implementation and local acceptance.** Shared provider-independent Search, paired original evidence, optional memory candidates, guarded release and additive GIN indexes are implemented. The 500-record diagnostic compares both representations under fixed limits and retains the original failure. All regression/browser, migration, persistence and visual checks are recorded in RUN.md. Publish to `dev` and verify the remote; this does not mark the live answer-quality gate complete.

- [x] **S09 implementation and local acceptance.** Correct/world change, preview/idempotent guarded commit, persistent source/passage exclusions, duplicate/reimport protection, worker/reply fencing, feedback diagnosis and one persisted retry are implemented. Ask/controls are connected to the browser. All 225 backend and 15 browser checks, migrations, data preservation and restart checks pass. [Actual evidence](RUN.md#s09-controls-and-synthetic-ask). Publish the coherent changes on `dev`; user-controlled merging remains unchanged. This does not certify live model quality.

- [x] **S10 measurement/UI implementation and local acceptance.** Explicit sample import, owner-scoped source/claim/passage byte counts, per-role/model known tokens versus unknown reservations, transient action/stage timing, new failed-call durations and isolated resource measurements are implemented. All 232 backend/17 browser tests, final targeted checks and complete application state preservation pass. [Actual evidence and resolved failures](RUN.md#s10-usage-and-performance). Available on `dev`; the successful live demonstration gate remains open.

- [x] **React workspace refinement.** User-approved Kivi/21st-informed design, shadcn foundation, exact typed-note imports, restrained request/result motion, accessible source/control dialogs, usage visibility and Private scratchpad/session clearing. Full existing PostgreSQL checks, 22 browser journeys, deployed shell and complete state preservation passed. [Acceptance record](eval/reports/react-workspace.json). No migration, provider/budget change or new live-model result.

## Ongoing

- [ ] **S06/S08 — Live answer baseline and comparison.** Lightning now has three completed cited answers; two meet the reviewed obligations, while the personalized draft misses the required three-bullet format. Ultra answered one of two questions and failed the other at the provider. Seven historical Kimi attempts still have zero completed answers. The controlled repeated representation comparison remains pending; source-only retrieval stays the default.

- [ ] **S07 — Live extraction quality.** The qualified-context Lightning repair preserves unknown event times but still leaves old/new launch dates active, rejects the conditional claim and omits some structured details. Ultra retained useful preference/conditional details but only four of seven attempted extraction jobs succeeded; the spending-conflict note was not called before its ten-call cap. Neither model closes the semantic quality gate. Original failed probes and exact sources remain retained.

- [ ] **S01 — Repository preservation/mechanical checks of final Part One.** Applicant reports the independently authored final documents complete and held separately. Earlier source notes and AI-assisted drafts retain their distinct provenance; the assistant has not inspected the finals.
  - Next action: preserve the final files when supplied, with actual dates/attribution, and check the positioning/vision limits (100/600 words). Do not generate or silently rewrite their content.
  - The user's latest explicit S03 approval supersedes the earlier implementation hold. No repeated confirmation is needed for routine S03 implementation/fixes.

- [x] **S10 — Bounded live interview demonstration.** The selected `my-notes` journey now demonstrates saved-source visibility, successful processing, source-backed Ask with per-request metrics, and context-free Private direct chat. This is a reliable prepared demo path, not a claim that every query, extraction or the partial 540-note corpus succeeds.

## Pending

| Step | Work | Evidence required before completion |
| --- | --- | --- |
| S06 | Finish the implemented Ask baseline’s live gate. | A cited answer/draft from actual stored sources; honest unknown/failure behavior; actual provider and usage recorded where permitted. |
| S07 | [Implementation verified; live gate open](PLAN.md#s07-bounded-proposal-after-research-review): selective memory and reconciliation are inspectable through the browser once processing is enabled. | Distinguish zero-memory decisions, new fact, genuine change, extraction error, tentative claim and unresolved conflict; verify worker replay/atomicity/Private; separately approved live proposer evaluation. |
| S08 | Retrieval implemented; live answer comparison remains open. | Local lexical/source-plus-memory diagnostic passed; optional dense comparison unrun. Finish S06 and compare live answers with the response model and evidence-token budget held fixed. |
| S09 | Implementation/local acceptance complete; monitor under later live evaluation. | Preserve the passing lifecycle/privacy contracts when improving model behavior. |
| S11 | Corpus/evaluator implemented: 540 paired fictional observations, 30 preselected showcases, 30 held-out and 24 development cases. Full live run remains pending. | Process the full corpus, retain all failures, run separate questions and report supported task success, latency, known/unknown tokens and storage growth. Same-author held-out data is not independent blind evidence. |
| S12 | Package and verify submission. | Clean checkout → Compose start → import → process → UI → tests/evaluation → scoped reset; README/RUN, results and exact tested commit ready. |

The S01–S12 IDs match the delivery sequence discussed with the user. PLAN.md groups implementation work into commit-sized milestones; one delivery step may span more than one honest commit. Tests are incremental throughout, not postponed to S11.

## Milestone completion procedure

1. Explain important implementation scope and its gate. Follow existing approval; ask only when a material change is outside that approved scope.
2. Implement the bounded milestone and run relevant checks. Use documentation checks for documentation, actual DB tests for persistence/concurrency and explicitly labeled live runs for model behavior.
3. Inspect the diff and results. Fix failures or record the limitation without marking the milestone passed. No credentials, private data or unrelated edits enter the commit.
4. Update this tracker, relevant decisions and run instructions with what changed, what was tested, the result and the next step.
5. Create a focused implementation commit on `dev` and push to remote `dev` after every meaningful completed milestone. The user has authorized this recurring workflow; do not ask again merely to push an approved milestone. The user reviews and merges into `main`, or explicitly instructs the assistant to merge and push, as authorized for S05. Do not create implementation commits directly on `main`.
6. Verify remote/local commit agreement and report the commit. If push fails, record that it remains local and resolve it without force-pushing over concurrent changes.

Preservation/tracker milestone checks on 11 September: UTF-8 decoding passed for nine Markdown files; all 26 then-present local links/anchors resolved; source-note counts matched 813/317; completed/ongoing/pending headings and S01–S12 coverage passed; staged whitespace passed. Environment checks are recorded in RUN.md; recheck documentation links and staged whitespace for that milestone. No application or model tests apply yet. The latest tracker commit is available through `git log --oneline -- todo.md`; avoid inventing a self-referential commit hash inside its own content.

Fresh-chat handoff checks: nine UTF-8 Markdown files, 30 local links/anchors, unique headings, preserved 83/378-word AI-assisted drafts, unchanged 813-token source notes and all eight referenced parent-workspace files passed. Staged whitespace is checked before committing the handoff. This verifies documentation continuity, not application behavior.

Visual-guide checks: 18 rendered pages and bookmarks, 207 non-overlapping node boxes, 57 PDF links, builder syntax, exact-source excerpt and RRF arithmetic passed. All pages received visual review; corrected diagrams were rendered and reviewed again. No clipping or remaining material diagram issues were found. Markdown links/anchors and staged whitespace are checked before the documentation commit. No application or live-model tests are claimed.

Visual-guide reference revision checks on 11 September: all 18 rendered pages were visually reviewed, including the final page-2 wording. The 54 reference cards map to 24 sources; all 129 PDF links have valid bounds and intended targets. Eighteen bookmarks, 207 non-overlapping node boxes, page-2 scope assertions, Python syntax, local Markdown/project-reference links and byte-identical PDF/register regeneration passed. Brief page locators and its SHA-256 were checked against the local original; primary technical sources were revisited. References explain methods or requirements and do not establish product correctness. No application or live-model tests ran.

Implementation-chat handoff checks on 11 September: nine UTF-8 Markdown files and all 32 pre-audit local links/anchors passed; preserved note/draft counts remain 813/317 and 83/378. Eight synthetic observations, eight separate cases and all 11 exact evidence excerpts passed, including intentionally missing metadata and conflicting amounts. All 635 earlier-work archive entries and five documentation-snapshot entries matched their SHA-256 manifests; ZIP integrity checks passed. The course's eight tests and the blueprint's static/interaction validator passed; the 18-page guide and seven-page brief were read and rendered for inspection, and the guide builder parsed successfully. These validate existing reference materials, not application, database or live-model behavior. Older parent-workspace plans and the archived SQLite probe do not supersede this repository's implementation plan. The next coding instruction is expected from the user's discussion chat; no bounded S03 approval was present in the reviewed handoff.

## Open before dependent work

- Repository inclusion and mechanical checks of the final Part One documents reported complete by the applicant; source-note preservation is already complete.
- Important implementation scopes remain subject to the user's review rule; do not treat approval of a step as approval for every later architecture change.
- Before important S06 code edits: review the concrete bounded proposal; settle the explicit trial-terms exception or compliant provider settings before live calls. S03–S05 are approved and implemented; the earlier Part One/S03 hold is superseded. Keep routine implementation on `dev` in the existing checkout and preserve other sessions' changes. S05's requested merge/push to `main` is authorized. Docker commands inside a restricted CLI subprocess require the appropriate approved execution context.
- Before live calls: provider credentials stored locally, account access, retention/no-training settings and an agreed spend/token ceiling. No secrets in chat or Git.
- Azure, graph services, response caching, streaming, autonomous procedural learning and rich UI polish remain optional after required gates pass.

## S14 current checkpoint: local workflow finalization (ongoing)

- Approved: shared lifetime 750 requests / 10M accounted tokens / $0 paid; final 30 requests reserved for demo. Old 96-call paragraph reconciled. Database and consumption preserved.
- Implemented, checks running: in-app 12-stage Workflow & evidence guide, selected-collection processing steps/pause, per-answer actual call metrics, curated synthetic report viewer, machine-readable storage contract and explicit Laguna reasoning backup selection.
- Live: all 540 originals imported into `submission-540-20260912`; extraction running with failures retained. Import success is not completion of learning. No full-corpus success claim.
- Pending: complete permitted extraction window, 30 real showcase cases with actual traces, fresh backend/browser regressions and local desktop/mobile journey, final clean local handoff and exact dev push.
- User approved unfamiliar Normal dummy-data inference after the NVIDIA warning. Reviewer mode is now enabled locally; no Private/lifecycle/budget guards were removed. The 12 `my-notes` records remain saved; active bulk learning can hold the shared owner lease.
- Part One originals remain preserved; independent-writing compliance and the Q&A submission boundary are unresolved, as recorded in the prior checkpoint.
- Next action: finish current build/checks, record backup diagnostic outcomes, then expose tested UI on localhost:8000 and complete the bounded live showcase evaluation.

- Fresh checks: 251 backend tests passed in 215.33s. Browser suite: 19/26 passed; seven failures are the same mobile overflow assertion caused by the added navigation item. Responsive-navigation and stale Pause-state fixes are awaiting the requested approval; no assertion was weakened.
- Live showcase is running. Early cases expose a real retrieval weakness: generic query terms can rank other projects above a requested project's later update. This is not a model-availability issue and requires separate general retrieval work, not fabricated positive evaluation feedback.

## S15 final-check checkpoint (12 September 2026, 23:04 IST)

Completed: 253 backend tests passed (194.57 s), all 26 browser journeys passed (60.14 s), edited Python files passed Ruff lint/format checks, and TypeScript/Vite built successfully. Real desktop/mobile inspection found no script errors or horizontal overflow (1440/1440 and 390/390 pixels). The Workflow & evidence page, storage map, collection-scoped processing, per-answer model-call metrics and supported-provider setup instructions are implemented.

Live evidence: the original 30-case S14 run is retained (13 mechanical screens; 4 operational failures). The S15 retest completed all 30 cases (16 mechanical screens; 3 operational failures; required sources retrieved and cited in 26 of 27 returned answers). These are not semantic accuracy scores. `eval/reports/s15-semantic-review.json` reviews all 30 outputs and identifies arithmetic errors, unsupported blocker labels, echo-only answers, status mismatches and the remaining Lagoon retrieval miss. No held-out/frozen-state quality claim is made.

Partial corpus gate: all 540 originals are stored, but full live processing is NOT complete. When the paced Google continuation was stopped to unblock interactive learning, the collection had 209 succeeded jobs (206 extracted, 3 no-memory), 46 failed, 284 pending and 1 interrupted lease. The 231 Google requests, failures, original-source states, storage snapshots and conservative unknown-usage reservation are retained. Do not claim 540 successfully processed records.

Current frontend diagnosis: `my-notes` contains 13 saved source records, but all 13 are pending learning. Source rows display record IDs; click a row to inspect its text. Ask questions are temporary requests, not silently saved observations. A fresh explicitly synthetic Birch note was saved through the actual browser and produced a correct source-cited live answer; its memory-learning check is awaiting the stopped bulk lease. Proposed source previews and an explicit save-question button await approval.

Known reporting limitations awaiting approval: the evaluator's stop-on-provider-failure branch can omit that last failure from one attempt summary; individual events/job states/call accounting still preserve it. A feedback-retry turn can retain the original duration display. Numeric substring screens are not a semantic judge and can accept 40 for 4; the qualitative review flags this baseline false positive.

Next: finish the live saved-note learning check, clean isolated Compose/persistence/reset rehearsal, package the final evidence, commit/push dev, then merge/push main under the user's explicit authorization. Part One independence and the boundary between the 387-word vision and its additional Q&A still need applicant confirmation; do not rewrite the applicant's text or call this complete compliance.

## S16 recovery checkpoint (12 September 2026, 23:18 IST)

User approved 1,100 total persisted requests, 10,000,000 accounted tokens, $0 paid, 30 requests reserved, with an 11:40 PM IST evaluation cutoff. The same budget key and consumed requests remain intact. Source and report code is pushed on dev at e78e6a4265cef3e64d5a4965ea59d84abffe2df3; main is not yet merged.

The Birch extraction failure was diagnosed: the model copied a JSON-schema $ref into content.value instead of returning a typed value. Validation rejected it. A bounded shared-service retry then produced a valid exact-source-linked memory retaining blue tin, second shelf and not the red tin. This is real model evidence in s16-extraction-diagnostic.jsonl, not a parser relaxation or fabricated success. The subsequent browser visibility check was interrupted by Docker's engine outage and must be rerun.

The resumed Google batch processed another 13 jobs before Docker Desktop's engine dropped the connection at 23:12:43 IST (unexpected EOF, exit 125). The last complete progress checkpoint was 218 succeeded, 47 failed and 275 pending; three additional successful events followed. Final database counts need recovery verification. The new test run could not start reliably because Docker returned 500/502 errors; s16-backend-tests.txt records that interruption, not a test pass. Container state reported exit 255 with OOMKilled=false; the cause is unconfirmed. A Docker Desktop restart has been proposed and awaits approval. No volume reset or deletion was performed.

## S16 final cutoff handoff (12 September 2026, 23:40 IST evaluation freeze)

- Completed: 540 frozen synthetic originals are stored in `submission-540-20260912`; repeated imports did not duplicate them. Final processing is 365 succeeded (328 extracted, 37 no-memory), 78 failed and 97 pending. The final batch exited after a Google `rate_limited` result at 23:38 IST. This is a partial processing run, not 540 successful memories.
- Completed: the final full backend suite passed 254 tests; the recovered browser suite passed all 26 journeys. The 78 targeted checks are a subset/repeat and are not added to the full-suite count. Ruff lint and format passed after test-file EOF normalization.
- Completed: real browser save, source inspection, bounded extraction retry, durable source-linked memory and a new live cited answer were demonstrated with the synthetic Birch adapter example. Initial invalid model output and operational failures remain in the evidence.
- Completed: clean isolated Compose start, source persistence across down/up and label-checked scoped reset were exercised. One standalone probe expected `matches` instead of the actual `matched` search status; its failure is retained alongside the successful visible search and persistence evidence. The main database was preserved.
- Completed: final read-only ledger is 720 requests and 5,857,878 accounted tokens against the approved 1,100 / 10,000,000 ceiling; 380 requests remain, including the 30-request demo reserve. Paid spend authorized is $0; actual provider billing is unknown. Application allowance does not guarantee provider quota.
- Evidence: `eval/reports/s14-readiness-review.json` is the final S16 brief crosswalk (legacy filename retained for the fixed browser report route). The original interrupted runs, model failures, both 30-case showcase runs and qualitative semantic review are preserved, not replaced by passing summaries.
- Pending after cutoff: 97 unprocessed records, 78 failed records requiring investigation/retry, the 30 held-out evaluations, and documented semantic errors. The 16/30 retest screen count is mechanical, not independently judged answer accuracy. No embedding/vector retrieval or guaranteed automatic fallback is claimed.
- Pending applicant decision: confirm the independently authored final Part One content boundary and word limits; the original Google Docs snapshot is preserved without rewriting.
- Next action: commit these final results on `dev`, push and verify, then perform the explicitly authorized fast-forward merge/push to `main`. The exact remotely verified submission SHA will be reported separately; this file does not claim a push before it happens. No additional live calls after the cutoff.
